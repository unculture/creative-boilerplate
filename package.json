{
  "private": true,
  "name": "creative-boilerplate",
  "version": "2.0.0",
  "description": "SmartContent Creative Boilerplate",
  "scripts": {
    "update": "npm install smartcontent-cdk; npm shrinkwrap --dev; gulp --updateCdk",
    "events": "gulp --browserEvents --production",
    "dev": "gulp watch",
    "prod": "gulp --production",
    "zip": "gulp zip --production"
  },
  "devDependencies": {
    "smartcontent-cdk": "git+ssh://git@bitbucket.org/dynamicdo/smartcontent-cdk.git#master"
  }
}
