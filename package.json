{
  "private": true,
  "name": "creative-boilerplate",
  "version": "0.7.0",
  "description": "SmartContent Creative Boilerplate",
  "scripts": {
    "upgrade": "npm install smartcontent-cdk; gulp --updateCdk",
    "events": "gulp --browserEvents --production",
    "dev": "gulp",
    "watch": "gulp watch",
    "prod": "gulp --production"
  },
  "devDependencies": {
    "smartcontent-cdk": "^0.7.0"
  }
}
